import 'package:flutter/material.dart';

/// Admin login screen. No dedicated route in Section 1's table —
/// this is the entry point before the shell/router take over.
class LoginPage extends StatelessWidget {
  const LoginPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Admin Login — pending design reference')),
    );
  }
}
